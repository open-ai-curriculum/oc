{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://open-ai-curriculum.local/domains/science/i3/learner-state.schema.json",
  "title": "I3 Learner State",
  "type": "object",
  "required": [
    "learner_id",
    "node_id",
    "status",
    "dependency_status",
    "direct_accuracy",
    "explanation_status",
    "transfer_status",
    "retention_status",
    "dominant_failure_modes",
    "recommended_interventions",
    "last_verified_at",
    "review_state"
  ],
  "properties": {
    "learner_id": { "type": "string", "minLength": 1 },
    "node_id": { "const": "I3" },
    "status": { "enum": ["blocked", "emerging", "provisional", "secure", "unstable"] },
    "dependency_status": {
      "type": "object",
      "required": ["I2"],
      "properties": {
        "I2": { "enum": ["secure", "unstable", "blocked"] }
      },
      "additionalProperties": false
    },
    "direct_accuracy": { "type": "number", "minimum": 0, "maximum": 1 },
    "explanation_status": { "enum": ["not_met", "weak", "met"] },
    "transfer_status": { "enum": ["not_met", "weak", "met"] },
    "retention_status": { "enum": ["not_due", "not_met", "met"] },
    "dominant_failure_modes": {
      "type": "array",
      "items": { "enum": ["I3-FM-01", "I3-FM-02", "I3-FM-03", "I3-FM-04"] },
      "uniqueItems": true
    },
    "recommended_interventions": {
      "type": "array",
      "items": { "type": "string", "minLength": 1 }
    },
    "last_verified_at": { "type": "string", "pattern": "^[0-9]{4}-[0-9]{2}-[0-9]{2}$" },
    "review_state": { "enum": ["draft", "human-reviewed"] },
    "notes": { "type": "string" }
  },
  "additionalProperties": false
}
