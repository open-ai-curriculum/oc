# M4 Intervention Playbook

- For `M4-FM-01`, use boundary-check routines that require all relevant stores and transfers to be named before model acceptance.
- For `M4-FM-02`, use cycle-reconstruction tasks where missing return paths must be added and justified.
- For `M4-FM-03`, use paired matter-versus-energy traces across the same system to force distinction.
- For `M4-FM-04`, use same-structure different-context comparisons across biology, chemistry, and Earth systems.
