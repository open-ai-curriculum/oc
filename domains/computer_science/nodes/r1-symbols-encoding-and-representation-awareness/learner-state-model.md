# R1 Learner State Model

The learner-state for `R1` tracks:

- dependency security on `D1`
- direct accuracy on symbols, encoding, and representation awareness tasks
- explanation quality for bounded representation reasoning reasoning
- transfer performance across unfamiliar computing contexts
- delayed retention outcome

`secure` requires all verification gates to pass. Any delayed failure or dependency instability should move the learner to `unstable`.
