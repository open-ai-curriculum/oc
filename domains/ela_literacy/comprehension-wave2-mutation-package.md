# Comprehension Wave 2 Mutation Package

## Purpose

Define the second canonical graph-fidelity mutation package for ELA/literacy by replacing the compressed comprehension branch with a higher-resolution node layer.

This package covers `R1` through `R3` and introduces `RC1` through `RC6`.
