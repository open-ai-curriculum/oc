# RC6 Intervention Playbook

## Purpose

Map `RC6` failure modes to bounded intervention responses.

## Intervention Rules

### For RC6-FM-01 Evidence Without Explanation

Primary interventions:

- tighten the task around evidence so the target is more visible
- keep the text or prompt surface small enough to isolate evidence without explanation
- rerun a bounded recheck on a changed but comparable text or prompt

Expected effect:

- learner resolves the dominant `evidence without explanation` pattern without changing the core node demand

Reverification target:

- one bounded changed-surface task

### For RC6-FM-02 Explanation Without Anchor

Primary interventions:

- tighten the task around explanation so the target is more visible
- keep the text or prompt surface small enough to isolate explanation without anchor
- rerun a bounded recheck on a changed but comparable text or prompt

Expected effect:

- learner resolves the dominant `explanation without anchor` pattern without changing the core node demand

Reverification target:

- one bounded changed-surface task

### For RC6-FM-03 Question-Type Confusion

Primary interventions:

- tighten the task around question-type so the target is more visible
- keep the text or prompt surface small enough to isolate question-type confusion
- rerun a bounded recheck on a changed but comparable text or prompt

Expected effect:

- learner resolves the dominant `question-type confusion` pattern without changing the core node demand

Reverification target:

- one bounded changed-surface task

### For RC6-FM-04 Transfer Fragility Across Texts

Primary interventions:

- tighten the task around transfer so the target is more visible
- keep the text or prompt surface small enough to isolate transfer fragility across texts
- rerun a bounded recheck on a changed but comparable text or prompt

Expected effect:

- learner resolves the dominant `transfer fragility across texts` pattern without changing the core node demand

Reverification target:

- one bounded changed-surface task

## Escalation Rule

If the same dominant failure mode remains blocking after two bounded intervention cycles, escalate to human review of dependency integrity, prompt design, and text-task alignment.
