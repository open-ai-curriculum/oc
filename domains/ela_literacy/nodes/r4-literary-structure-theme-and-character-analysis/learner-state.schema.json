{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "R4 Learner State",
  "type": "object",
  "required": [
    "learner_id",
    "node_id",
    "status",
    "dependency_status",
    "direct_accuracy",
    "explanation_status",
    "transfer_status",
    "retention_status",
    "dominant_failure_modes",
    "recommended_interventions",
    "last_verified_at",
    "review_state",
    "notes"
  ],
  "properties": {
    "learner_id": { "type": "string" },
    "node_id": { "const": "R4" },
    "status": {
      "type": "string",
      "enum": ["blocked", "emerging", "provisional", "secure", "unstable"]
    },
    "dependency_status": {
      "type": "object",
      "required": ["RC6"],
      "properties": {
        "RC6": {
          "type": "string",
          "enum": ["blocked", "emerging", "provisional", "secure", "unstable"]
        }
      },
      "additionalProperties": false
    },
    "direct_accuracy": { "type": "number", "minimum": 0, "maximum": 1 },
    "explanation_status": {
      "type": "string",
      "enum": ["not_met", "weak", "met"]
    },
    "transfer_status": {
      "type": "string",
      "enum": ["not_met", "weak", "met"]
    },
    "retention_status": {
      "type": "string",
      "enum": ["not_due", "not_met", "met"]
    },
    "dominant_failure_modes": {
      "type": "array",
      "items": {
        "type": "string",
        "enum": ["R4-FM-01", "R4-FM-02", "R4-FM-03", "R4-FM-04"]
      }
    },
    "recommended_interventions": {
      "type": "array",
      "items": { "type": "string" }
    },
    "last_verified_at": { "type": "string", "format": "date" },
    "review_state": {
      "type": "string",
      "enum": ["draft", "human-reviewed"]
    },
    "notes": { "type": "string" }
  },
  "additionalProperties": false
}
