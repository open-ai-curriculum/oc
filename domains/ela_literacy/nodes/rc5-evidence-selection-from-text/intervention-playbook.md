# RC5 Intervention Playbook

## Purpose

Map `RC5` failure modes to bounded intervention responses.

## Intervention Rules

### For RC5-FM-01 Answer Without Evidence

Primary interventions:

- tighten the task around answer so the target is more visible
- keep the text or prompt surface small enough to isolate answer without evidence
- rerun a bounded recheck on a changed but comparable text or prompt

Expected effect:

- learner resolves the dominant `answer without evidence` pattern without changing the core node demand

Reverification target:

- one bounded changed-surface task

### For RC5-FM-02 Irrelevant Detail Selection

Primary interventions:

- tighten the task around irrelevant so the target is more visible
- keep the text or prompt surface small enough to isolate irrelevant detail selection
- rerun a bounded recheck on a changed but comparable text or prompt

Expected effect:

- learner resolves the dominant `irrelevant detail selection` pattern without changing the core node demand

Reverification target:

- one bounded changed-surface task

### For RC5-FM-03 Evidence Overload

Primary interventions:

- tighten the task around evidence so the target is more visible
- keep the text or prompt surface small enough to isolate evidence overload
- rerun a bounded recheck on a changed but comparable text or prompt

Expected effect:

- learner resolves the dominant `evidence overload` pattern without changing the core node demand

Reverification target:

- one bounded changed-surface task

### For RC5-FM-04 Oral-Language Explanation Confound

Primary interventions:

- tighten the task around oral-language so the target is more visible
- keep the text or prompt surface small enough to isolate oral-language explanation confound
- rerun a bounded recheck on a changed but comparable text or prompt

Expected effect:

- learner resolves the dominant `oral-language explanation confound` pattern without changing the core node demand

Reverification target:

- one bounded changed-surface task

## Escalation Rule

If the same dominant failure mode remains blocking after two bounded intervention cycles, escalate to human review of dependency integrity, prompt design, and text-task alignment.
