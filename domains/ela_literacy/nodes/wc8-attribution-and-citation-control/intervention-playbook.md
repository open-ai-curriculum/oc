# WC8 Intervention Playbook

## Purpose

Map `WC8` failure modes to bounded intervention responses.

## Intervention Rules

### For WC8-FM-01 Attribution Omission

Primary interventions:

- tighten the task around attribution omission so the target is more visible
- keep the response surface bounded enough to isolate the real bottleneck
- rerun a changed-surface check after one short intervention cycle

Expected effect:

- learner resolves the dominant failure pattern without lowering the node demand

Reverification target:

- one bounded changed-surface task

### For WC8-FM-02 Citation-Format Instability

Primary interventions:

- tighten the task around citation-format instability so the target is more visible
- keep the response surface bounded enough to isolate the real bottleneck
- rerun a changed-surface check after one short intervention cycle

Expected effect:

- learner resolves the dominant failure pattern without lowering the node demand

Reverification target:

- one bounded changed-surface task

### For WC8-FM-03 Overformatted-But-Unclear Attribution

Primary interventions:

- tighten the task around overformatted-but-unclear attribution so the target is more visible
- keep the response surface bounded enough to isolate the real bottleneck
- rerun a changed-surface check after one short intervention cycle

Expected effect:

- learner resolves the dominant failure pattern without lowering the node demand

Reverification target:

- one bounded changed-surface task

### For WC8-FM-04 Mechanics-Overload Interference

Primary interventions:

- tighten the task around mechanics-overload interference so the target is more visible
- keep the response surface bounded enough to isolate the real bottleneck
- rerun a changed-surface check after one short intervention cycle

Expected effect:

- learner resolves the dominant failure pattern without lowering the node demand

Reverification target:

- one bounded changed-surface task

## Escalation Rule

If the same dominant failure mode remains blocking after two bounded intervention cycles, escalate to human review of dependency integrity, prompt design, and task-surface validity.
