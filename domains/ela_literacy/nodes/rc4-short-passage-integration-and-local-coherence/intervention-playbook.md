# RC4 Intervention Playbook

## Purpose

Map `RC4` failure modes to bounded intervention responses.

## Intervention Rules

### For RC4-FM-01 Sentence-Island Reading

Primary interventions:

- tighten the task around sentence-island so the target is more visible
- keep the text or prompt surface small enough to isolate sentence-island reading
- rerun a bounded recheck on a changed but comparable text or prompt

Expected effect:

- learner resolves the dominant `sentence-island reading` pattern without changing the core node demand

Reverification target:

- one bounded changed-surface task

### For RC4-FM-02 Local Coherence Drift

Primary interventions:

- tighten the task around local so the target is more visible
- keep the text or prompt surface small enough to isolate local coherence drift
- rerun a bounded recheck on a changed but comparable text or prompt

Expected effect:

- learner resolves the dominant `local coherence drift` pattern without changing the core node demand

Reverification target:

- one bounded changed-surface task

### For RC4-FM-03 Fluency-Comprehension Confound

Primary interventions:

- tighten the task around fluency-comprehension so the target is more visible
- keep the text or prompt surface small enough to isolate fluency-comprehension confound
- rerun a bounded recheck on a changed but comparable text or prompt

Expected effect:

- learner resolves the dominant `fluency-comprehension confound` pattern without changing the core node demand

Reverification target:

- one bounded changed-surface task

### For RC4-FM-04 Detail Overload

Primary interventions:

- tighten the task around detail so the target is more visible
- keep the text or prompt surface small enough to isolate detail overload
- rerun a bounded recheck on a changed but comparable text or prompt

Expected effect:

- learner resolves the dominant `detail overload` pattern without changing the core node demand

Reverification target:

- one bounded changed-surface task

## Escalation Rule

If the same dominant failure mode remains blocking after two bounded intervention cycles, escalate to human review of dependency integrity, prompt design, and text-task alignment.
