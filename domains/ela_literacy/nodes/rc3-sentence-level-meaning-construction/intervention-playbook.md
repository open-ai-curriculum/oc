# RC3 Intervention Playbook

## Purpose

Map `RC3` failure modes to bounded intervention responses.

## Intervention Rules

### For RC3-FM-01 Word-Calling Without Meaning

Primary interventions:

- tighten the task around word-calling so the target is more visible
- keep the text or prompt surface small enough to isolate word-calling without meaning
- rerun a bounded recheck on a changed but comparable text or prompt

Expected effect:

- learner resolves the dominant `word-calling without meaning` pattern without changing the core node demand

Reverification target:

- one bounded changed-surface task

### For RC3-FM-02 Referent Tracking Loss

Primary interventions:

- tighten the task around referent so the target is more visible
- keep the text or prompt surface small enough to isolate referent tracking loss
- rerun a bounded recheck on a changed but comparable text or prompt

Expected effect:

- learner resolves the dominant `referent tracking loss` pattern without changing the core node demand

Reverification target:

- one bounded changed-surface task

### For RC3-FM-03 Syntax Interpretation Fragility

Primary interventions:

- tighten the task around syntax so the target is more visible
- keep the text or prompt surface small enough to isolate syntax interpretation fragility
- rerun a bounded recheck on a changed but comparable text or prompt

Expected effect:

- learner resolves the dominant `syntax interpretation fragility` pattern without changing the core node demand

Reverification target:

- one bounded changed-surface task

### For RC3-FM-04 Decoding-Load Confound

Primary interventions:

- tighten the task around decoding-load so the target is more visible
- keep the text or prompt surface small enough to isolate decoding-load confound
- rerun a bounded recheck on a changed but comparable text or prompt

Expected effect:

- learner resolves the dominant `decoding-load confound` pattern without changing the core node demand

Reverification target:

- one bounded changed-surface task

## Escalation Rule

If the same dominant failure mode remains blocking after two bounded intervention cycles, escalate to human review of dependency integrity, prompt design, and text-task alignment.
