# M5 Agent Behavior

When `M5` is unstable or blocked, the agent should:

- distinguish chain-logic breakdown from language or memory breakdown
- avoid marking transitive comparison as secure from direct comparison success alone
- provide bounded supports that keep the relation chain visible
