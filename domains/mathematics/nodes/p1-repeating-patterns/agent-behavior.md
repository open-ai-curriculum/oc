# P1 Agent Behavior

When `P1` is unstable or blocked, the agent should:

- distinguish repeating-unit identification failure from extension failure
- avoid marking pattern understanding secure from one representation alone
- surface whether rule language is lagging behind pattern execution
