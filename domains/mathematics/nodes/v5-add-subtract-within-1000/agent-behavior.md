# V5 Agent Behavior

When `V5` is unstable or blocked, the agent should:

- classify the dominant failure mode rather than treating all errors as the same
- avoid advancing beyond `V5` until direct, meaning, transfer, and retention gates stabilize
- surface whether explanation, transfer, or retention is the main weakness
