# D1 Agent Behavior

When `D1` is unstable or blocked, the agent should:

- distinguish sorting execution failure from rule-switching failure
- avoid treating one successful sort as evidence of flexible classification
- surface whether rule explanation lags behind performance
