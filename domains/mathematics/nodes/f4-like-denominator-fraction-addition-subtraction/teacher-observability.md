# F4 Teacher Observability View

Teachers should see:

- current `F4` status
- dependency status for `F1` and `F3`
- whether errors come from denominator misuse, improper-fraction interpretation, or shallow procedure
