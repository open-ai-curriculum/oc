# G1 Agent Behavior

When `G1` is unstable or blocked, the agent should:

- distinguish shape-identity weakness from orientation dependence
- avoid treating memorized shape names as secure if classification remains superficial
- surface whether solid-shape recognition lags behind flat-shape recognition
