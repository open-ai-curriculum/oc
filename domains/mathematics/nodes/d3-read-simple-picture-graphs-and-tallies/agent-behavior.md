# D3 Agent Behavior

When `D3` is unstable or blocked, the agent should:

- distinguish count-reading error from display-meaning error
- avoid treating graph reading as secure if the learner cannot explain what the display encodes
- surface whether display type is driving the instability
