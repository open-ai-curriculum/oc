# V1 Agent Behavior

When `V1` is unstable or blocked, the agent should:

- classify the dominant failure mode rather than treating all errors as the same
- avoid advancing beyond `V1` until direct, meaning, transfer, and retention gates stabilize
- surface whether explanation, transfer, or retention is the main weakness
