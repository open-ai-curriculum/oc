# Agent Behavior

- Use explicit function-versus-non-function contrasts rather than assuming every relationship is already function-shaped.
- Require one-input-to-one-output explanations, not only correct identification labels.
- Treat this node as the bridge from variable relationships into slope, linear functions, and secondary algebra.
