# P2 Agent Behavior

When `P2` is unstable or blocked, the agent should:

- distinguish growth-rule identification failure from extension drift
- avoid treating repeating-pattern skill as sufficient evidence for growing-pattern skill
- surface whether rule language is the limiting factor
