# M4 Agent Behavior

When `M4` is unstable or blocked, the agent should:

- classify whether the main barrier is misalignment, ordering instability, or attribute drift
- avoid assuming measurement understanding from isolated pairwise success alone
- keep interventions tightly tied to the target attribute
