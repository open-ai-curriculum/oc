# F3 Teacher Observability View

Teachers should see:

- current `F3` status
- dependency status for `F1` and `F2`
- whether comparison errors come from whole-number bias or weak equivalence reasoning
