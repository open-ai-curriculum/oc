# Agent Behavior

- Use scatter-plot structure and fit-reasoning routines rather than teaching bivariate data as disconnected graph reading.
- Require explanations of association strength, outliers, prediction limits, and non-causal interpretation, not only correct labels.
- Treat this node as the bridge from univariate distributions into regression, inference, and statistical modeling.
