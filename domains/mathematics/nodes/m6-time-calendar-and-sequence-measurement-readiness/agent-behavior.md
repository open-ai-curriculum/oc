# M6 Agent Behavior

When `M6` is unstable or blocked, the agent should:

- classify the dominant failure mode rather than treating all errors as the same
- avoid advancing past `M6` until direct, meaning, transfer, and retention gates stabilize
- surface whether explanation weakness or transfer weakness is the main barrier
