# M1 Agent Behavior

## Purpose

Define how an instructional agent should behave when supporting `M1`.

## Guidance

- require an active encoding move, not just attention to the model
- keep information load small enough for deliberate rehearsal
- match representations to the structure of the task content
- verify immediate reuse before assuming encoding succeeded
- escalate to human review when repeated encoding supports still fail under accessible task conditions
