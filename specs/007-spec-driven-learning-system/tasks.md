# Tasks: Spec-Driven Learning System

## Phase 1. Repository Reset

- [x] rewrite the constitution around the spec-driven learning model
- [x] rewrite agent guidance around the new authority order
- [x] rewrite the top-level README to reflect the hard pivot
- [x] create the primary system spec package

## Phase 2. First Domain Package

- [x] create the mathematics domain package
- [x] define the capability graph for the first slice
- [x] define the first DSL or machine-readable schema for the slice
- [x] specify the first node package for unlike-denominator fraction addition and subtraction

## Phase 3. Operationalization

- [x] define the learner-state model in executable or near-executable form
- [x] define the verification model for the first node
- [x] define the intervention playbook for the first node
- [x] define the teacher-facing observability view for the first slice

## Phase 4. Upstream Spine Expansion

- [x] define one authoritative master knowledge graph for the domain
- [x] define the zero-state to `F1` predecessor spine
- [x] define a staged upstream build plan
- [x] build the direct `F1` predecessor nodes `R1`, `R2`, and `R3`
- [x] revise `F1` so it reflects its true direct dependencies once `R1` through `R3` exist
- [x] build the immediate implemented predecessors to `R1`: `N1`, `U4`, and `M3`
- [x] revise `R1` so its upstream dependency set points to implemented node packages inside the repository
- [x] build the immediate implemented predecessors to `N1`, `U4`, and `M3`: `C3`, `U3`, and `M2`
- [x] revise `N1`, `U4`, and `M3` so their upstream dependency sets point to implemented node packages inside the repository
- [x] build the immediate implemented predecessors to `C3`, `U3`, and `M2`: `C2`, `U2`, and `M1`
- [x] revise `C3`, `U3`, and `M2` so their upstream dependency sets point to implemented node packages inside the repository
- [x] build the immediate implemented predecessors to `C2`, `U2`, and `M1`: `C1`, `U1`, and `C4`
- [x] revise `C2`, `U2`, and `M1` so their upstream dependency sets point to implemented node packages inside the repository
- [x] build the zero-state entry floor: `Z1`, `Z2`, and `Z3`
- [x] revise `C1` and the master graph entry boundary so the novice-to-fraction path is explicitly continuous from zero state
- [x] build the whole-number operations bridge nodes `N2`, `N3`, and `N4`
- [x] revise the novice-to-fraction implementation map so the whole-number branch is shown as an implemented contiguous segment through `N4`
- [x] build the base-ten readiness nodes `N5` and `N6`
- [x] revise the novice-to-fraction implementation map so the whole-number branch is shown as an implemented contiguous segment through `N6`
- [x] build the measurement comparison nodes `M4` and `M5`
- [x] revise the master graph implementation narrative so the measurement branch is shown as implemented through indirect comparison
- [x] build the equal-group and equal-share readiness nodes `U5` and `U6`
- [x] revise the master graph implementation narrative so the unitizing branch is shown as implemented through `U6`
- [x] build the first geometry and spatial-reasoning nodes `G1`, `G2`, and `G3`
- [x] revise the master graph implementation narrative so the geometry branch is shown as implemented through `G3`
- [x] build the first pattern and algebraic-readiness nodes `P1`, `P2`, and `P3`
- [x] revise the master graph implementation narrative so the pattern branch is shown as implemented through `P3`
- [x] build the first data and representation nodes `D1`, `D2`, `D3`, and `D4`
- [x] revise the master graph implementation narrative so the data branch is shown as implemented through `D4`
- [x] build the remaining modeled peer nodes `P4`, `G4`, `G5`, `M6`, and `M7`
- [x] revise the master graph implementation narrative so the non-fraction peer branches are shown as implemented through their current frontier
- [x] define and document the current frontier boundary at `F5` and defer `S1` through `E3`
- [x] extend the authoritative graph so it includes the missing broad elementary-math branches that still precede or sit alongside `F5`
- [x] implement the place-value and multidigit whole-number branch `V1` through `V5`
- [x] implement the multiplicative reasoning and division branch `T1` through `T4`
- [x] implement the advanced pre-frontier measurement branch `M8` through `M11`
- [x] implement the number-line and linear-magnitude branch `L1` through `L4`

## Phase 5. Legacy Reconciliation

- [x] remove prior curriculum-commercialization artifacts from the active repository direction
- [x] remove obsolete curriculum-product specs and curriculum-specific validator/test layers
- [x] update the governing docs so the new direction stands alone

## Phase 6. Downstream Extension

- [x] implement the signed-number meaning and operations nodes `S1` and `S2`
- [x] implement the equation-solving nodes `E1` through `E3`

## Phase 7. Fraction-Peer Coverage Expansion

- [x] extend the master graph with a decimal branch `X1` through `X5`
- [x] extend the multiplicative branch with `T5` through `T7`
- [x] extend measurement with `M12` through `M14`
- [x] extend geometry with `G6` and `G7`
- [x] extend data representation with `D5` and `D6`
- [x] add estimation and reasonableness branch `Y1` through `Y4`
- [x] revise the peer-level graph documents so they treat these branches as required pre-frontier mathematics territory

## Phase 8. Structural Refinement

- [x] add explicit early numeral-representation nodes for numeral recognition/writing and quantity-symbol correspondence
- [x] route symbolic teen-number work through the new numeral-representation bridge
- [x] add an explicit fraction-greater-than-one magnitude node
- [x] route bounded mixed-number and improper-fraction number-line/operation work through that new magnitude node
- [x] tighten unit-conversion dependencies so measurement conversion depends on explicit multiplicative structure
- [x] remove non-essential time-readiness dependency from angle-as-turn geometry work
- [x] complete dedicated cross-branch integration audit for the pre-fraction frontier

## Phase 9. Downstream Frontier Expansion

- [x] model the next downstream branches beyond the fraction-led frontier
- [x] define ratio and proportional reasoning nodes `Q1` through `Q4`
- [x] define downstream algebra and coordinate nodes `A1` through `A5`
- [x] author the downstream expansion plan and sequencing rationale
- [x] choose the first downstream implementation target after `E3`
- [x] implement `Q1` ratio as multiplicative comparison
- [x] implement `Q2` unit rate reasoning
- [x] implement `Q3` proportional relationships
- [x] implement `Q4` percent as rate per hundred
- [x] implement `A1` variable expressions and substitution
- [x] implement `A2` distributive reasoning and combining like terms
- [x] implement `A3` inequalities and solution sets
- [x] implement `A4` coordinate plane and ordered pairs
- [x] implement `A5` linear relationship tables and graphs

## Phase 10. Full K-12 Mathematics Expansion Architecture

- [x] define the K-12 and AP target architecture for the mathematics domain
- [x] define the phase-based expansion plan from the current implemented frontier to full K-12/AP scope
- [x] revise the mathematics domain docs so they state the true K-12/AP target rather than only the earlier proof-of-method scope
- [x] define the next unmodeled middle-school-to-secondary strand families in the master graph
- [x] define the dependency bridges from the current frontier into secondary algebra, geometry, statistics, and advanced mathematics

## Phase 11. Middle-School To Secondary Modeling

- [x] define the detailed node sequence for full Grade 7 and Grade 8 completion
- [x] define the first governed implementation targets for the `J` and `K` families
- [x] define the first governed implementation targets for the `H` and `W` families
- [x] implement `J1` rational-number addition and subtraction
- [x] implement `J2` rational-number multiplication and division
- [x] implement `J3` signed rational-number fluency and order
- [x] implement `J4` numerical expressions and absolute value
- [x] implement `J5` percent change, discount, tax, and markup
- [x] implement `J6` proportional application and scale modeling
- [x] implement `K1` variable relationships and independent/dependent quantities
- [x] implement `K2` function as input-output rule
- [x] implement `K3` slope, rate of change, and linear growth
- [x] implement `K4` linear equations in two variables
- [x] implement `K5` linear functions in multiple representations
- [x] implement `K6` systems of linear equations and inequalities
- [x] implement `K7` quadratic foundations
- [x] implement `H1` transformations and coordinate symmetry
- [x] implement `H2` congruence and similarity via transformations
- [x] implement `H3` proof structure and geometric argument
- [x] implement `H4` right-triangle trigonometric ratio foundations
- [x] implement `H5` circles, measurement, and coordinate geometry
- [x] implement `W1` statistical distributions and variability
- [x] implement `W2` bivariate data and linear association
- [x] implement `W3` sampling, inference, and study design
- [x] implement `W4` probability models and compound events
- [x] implement `B1` polynomial structure and operations
- [x] implement `B2` rational, radical, and exponential functions
- [x] implement `B3` logarithmic and inverse-function reasoning
- [x] implement `B4` trigonometric functions and unit circle
- [x] implement `B5` precalculus function synthesis and limits foundations
- [x] implement `AP1` AP Statistics extension
- [x] implement `AP2` AP Precalculus extension
- [x] implement `AP3` calculus limits and derivatives
- [x] implement `AP4` integral accumulation and Fundamental Theorem
- [x] implement `AP5` BC series and advanced calculus extensions
